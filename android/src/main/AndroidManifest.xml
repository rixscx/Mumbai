<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- There is no <uses-permission android:name="android.permission.INTERNET" /> here, and that
         omission is the point. The privacy promise ("your data never leaves the phone unless you
         export it") is enforced by the app being incapable of network I/O, not by a policy note.
         When a live-hours feature lands it will need this permission and that will be a visible,
         reviewable diff rather than a capability that was always quietly present. -->

    <application
        android:name=".MumbaiApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mumbai">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Mumbai">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
